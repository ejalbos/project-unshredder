desc "`rake` will default to running the app"
task :default => :run

#desc "Run all the rspec examples"
#task :spec do
#  system "bundle exec rspec -c spec/*_spec.rb"
#end

desc "Run the 'imgus' app"
task :run do
  system "bundle exec ruby lib/imgus.rb"
end